#include<stdio.h>
#include<string.h>
int main()
{
	char s[100];
	printf("enter string: ");
	gets(s);
	int i;
	for(i=0;s[i]!='\0';i++)
	{
		if(s[i]=='a')
			s[i]=s[i]-32;
	}
	printf("output: ");
	puts(s);
	return 0;
}
---------
OUTPUT:
---------
enter string: aditya
output: AdityA
